name: Apply SourceFacts closeout patch v2

on:
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: agent/sourcefacts-closeout-20260818
          fetch-depth: 0
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: '3.11'
          cache: pip
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Reuse staged patch
        run: |
          python - <<'PY'
          from pathlib import Path
          source = Path('.github/workflows/apply-sourcefacts-closeout.yml').read_text(encoding='utf-8')
          marker = "          python - <<'PY'\n"
          start = source.index(marker) + len(marker)
          end = source.index("\n          PY\n", start)
          lines = source[start:end].splitlines()
          code = '\n'.join(line[10:] if line.startswith('          ') else line for line in lines) + '\n'
          Path('/tmp/apply_sourcefacts_closeout.py').write_text(code, encoding='utf-8')
          PY
          python /tmp/apply_sourcefacts_closeout.py
      - name: Targeted regressions
        run: |
          python -m pytest -q \
            tests/test_source_facts_summary_reliability.py \
            tests/test_source_facts_quality_telemetry.py \
            tests/test_sourcefacts_guarded_backfill.py
      - name: Full unit suite
        run: python -m pytest -q
      - name: Golden and structural gates
        run: |
          python scripts/evaluate_dedup_golden.py
          python scripts/evaluate_golden.py --details /tmp/golden.json.csv --json /tmp/golden.json
          python -m cyberwatch test-repeat
          python -m cyberwatch check
      - name: Commit validated code patch
        run: |
          git config user.name "cyberwatch-bot"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            cyberwatch/source_facts_ai.py \
            cyberwatch/model.py \
            cyberwatch/status.py \
            cyberwatch/runner.py \
            scripts/backfill_source_fact_summaries.py \
            scripts/run_sourcefacts_backfill_guarded.py \
            scripts/report_source_performance.py \
            tests/test_source_facts_summary_reliability.py \
            tests/test_sourcefacts_guarded_backfill.py
          git commit -m "Decouple historical SourceFacts null retries from CREATE"
          git push origin HEAD:agent/sourcefacts-closeout-20260818
